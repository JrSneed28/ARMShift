# ARMShift
Generic ARM64 (aarch64) architecture differential harness — a bounded, versioned instruction corpus run natively on genuine ARM64 silicon (GitHub-hosted ubuntu-24.04-arm) as an independent semantic reference, with a fail-closed diff engine and a seeded negative control.
